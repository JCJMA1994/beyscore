name: bey_domain
description: Pure Dart domain layer for BeyScore - entities, value objects, and deterministic domain services.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  equatable: ^2.0.7
  fpdart: ^1.1.0
  crypto: ^3.0.6
  uuid: ^4.5.1

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^10.2.0
